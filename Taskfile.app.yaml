version: '3'

tasks:
  start:
    desc: Rebuild the Docker image
    cmds:
      - docker compose build
      - docker compose up -d

  stop:
    desc: Stop the Docker container
    cmds:
      - docker compose down
